# 课程智慧教学平台 (Course Teaching Platform)

## 1. 项目概览 (Project Overview)
课程智慧教学平台是一个集“教、学、练、测、评”于一体的现代化教学辅助系统。平台基于 RBAC 模型设计，服务于系统管理员、教师和学生三种角色，深度集成 AI 助教与自动评测功能，旨在通过技术手段降低教学负担，提升学生学习体验。

### 核心亮点
- **全流程教学管理**：覆盖课程、班级、作业、考试全生命周期。
- **AI 赋能教学**：内置 AI 助教（聊天机器人）、AI 作业批改、代码智能纠错。
- **沉浸式实验环境**：提供在线 Web IDE，支持代码编写、运行、测试用例自动评测。
- **可视化知识图谱**：构建课程知识体系，直观展示知识点关联与学习路径。

---

## 2. 技术架构 (Technical Architecture)

本项目采用现代化的前后端分离架构，追求高性能与开发体验。

### 前端 (Frontend)
- **核心框架**: React 19, Vite 6
- **UI 组件库**: Shadcn/UI, TailwindCSS 4 (样式与响应式设计)
- **状态管理**: TanStack Query (React Query)
- **可视化**: React Flow (知识图谱), Recharts (数据报表)
- **编辑器**: Monaco Editor (Web IDE 体验)
- **API 通信**: tRPC Client (端到端类型安全)

### 后端 (Backend)
- **运行环境**: Node.js (Express)
- **API 框架**: tRPC Server (这也是本项目的一大特色，实现了前后端类型共享)
- **数据库 ORM**: Drizzle ORM (TypeScript-first schema design)
- **数据库**: MySQL 8.0+

### 工具链 (Tools)
- **包管理**: pnpm
- **构建工具**: Vite + esbuild
- **类型检查**: TypeScript 5.9

---

## 3. 项目结构 (Project Structure)

```
course-teaching-platform/
├── client/                 # 前端应用源码
│   ├── src/
│   │   ├── components/     # 通用组件 (UI, Layouts)
│   │   ├── pages/          # 页面视图 (区分 student/teacher 路由)
│   │   ├── lib/            # 工具函数与 API 客户端
│   │   └── App.tsx         # 路由配置与应用入口
├── server/                 # 后端应用源码
│   ├── routers/            # tRPC 路由定义 (API Endpoints)
│   ├── services/           # 业务逻辑层
│   ├── db.ts               # 数据库连接配置
│   └── index.ts            # 服务器入口
├── drizzle/                # 数据库及迁移文件
│   ├── schema.ts           # Drizzle 数据库模式定义
│   └── *.sql               # SQL 迁移脚本
├── .env                    # 环境变量配置
└── package.json            # 项目依赖与脚本
```

---

## 4. 核心功能 (Key Features)

### 👤 用户权限 (RBAC)
- **管理员**: 教师账户管理、系统监控。
- **教师**: 班级管理、课程建设、发布作业/实验、批阅成绩。
- **学生**: 在线学习、提交作业、查看成绩、AI 辅助学习。

### 📚 教学管理
- **课程与班级**: 支持 Excel 批量导入学生，灵活分班管理。
- **作业系统**: 支持发布普通作业，不仅支持人工批阅，更集成了 **AI 辅助评分**，大幅提高批改效率。
- **考试管理**: 在线考试系统，支持题库组卷、自动倒计时收卷。

### 🧪 智能实验 (Intelligent Lab)
- **在线编程**: 集成 Monaco Editor，支持代码高亮与补全。
- **自动评测**: 教师配置测试用例，系统后台沙箱运行并反馈结果 (Pass/Fail)。
- **智能纠错**: AI 分析学生代码，提供非直接答案的引导性建议。

### 🕸 知识图谱 (Knowledge Graph)
- **知识可视化**: 如果程章节与知识点以图谱形式展示。
- **关联学习**: 题目、作业可关联具体知识点，帮助学生构建知识网络。

---

## 5. 快速开始 (Getting Started)

### 环境要求
- Node.js >= 18
- MySQL >= 8.0
- pnpm

### 安装步骤

1. **克隆项目**
   ```bash
   git clone <repository-url>
   cd course-teaching-platform
   ```

2. **安装依赖**
   ```bash
   pnpm install
   ```

3. **配置环境变量**
   复制 `.env.example` 为 `.env`，并配置数据库连接信息：
   ```env
   DATABASE_URL="mysql://user:password@localhost:3306/course_teaching_platform"
   ```

4. **初始化数据库**
   推送 Schema 到数据库：
   ```bash
   npm run db:push
   ```
   (可选) 填充测试数据：
   ```bash
   npm run seed
   ```

5. **启动开发服务器**
   ```bash
   npm run dev
   ```
   启动后，前端通常运行在 `http://localhost:3000` (或 3001/3002)。

---

## 6. 许可证 (License)
MIT License.
